#include <iostream>
#include <vector>

using namespace std;

vector<int> solution(int n)
{
	vector<int> answer;

	return answer;
}

int main()
{
	vector<int> result = solution(2);

	for (int i = 0; i < result.size(); ++i)
		cout << result[i] << ", ";

	cout << endl << endl;

	system("pause");
	return 0;
}
